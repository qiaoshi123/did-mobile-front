# 2026-03-17

## 创建路由规范 Rule（06-路由规范.md）

**文件**：`.codebuddy/rules/06-路由规范.md`

**内容涵盖**：
- 路径常量管理（`src/constants/routes.ts`，`PAGE_` 前缀 + UPPER_SNAKE_CASE）
- pages.json 配置约束（页面注册、tabBar、分包 subPackages、预下载 preloadRule）
- 路由跳转规范（5 种跳转方式的场景选择、`router` 工具函数封装在 `src/utils/route.ts`）
- 路由守卫（`src/utils/route-guard.ts`，`uni.addInterceptor` 拦截，白名单机制）
- 禁止事项（禁止硬编码路径、禁止直接调 uni 原生跳转 API、禁止循环跳转等）

**设计原则**：Rule 只写项目级约束和决策标准，不重复 uni-app 官方文档的 API 说明。pages.json 的动态配置（如 navigationStyle、enablePullDownRefresh）因页面而异，不写死在 Rule 里，由 AI 在创建页面时根据需求自行判断。

**联动变更**：
1. `03-项目结构.md` — "页面路由约束"段落精简，指向 `06-路由规范.md`，新增分包目录位置说明
2. `create-page` Skill — 必须读取的 Rule 列表新增 `06-路由规范.md`

## 修正路由守卫多端支持说明

查阅 uni-app 官方文档后，修正了 `06-路由规范.md` 中路由守卫部分：

- `uni.addInterceptor` **全端支持**（App / H5 / 小程序）
- 原来写的"switchTab 不支持拦截器"不准确，实际是：代码调用 `uni.switchTab` 可以被拦截，但小程序端用户**点击底部 tabBar** 的底层不触发 `uni.switchTab`，所以拦截器无效
- 拦截列表新增了 `switchTab`（代码调用可拦截）
- 新增了 tabBar 页面 `onShow` 兜底权限校验的示例代码
- 建议封装 `useAuthGuard` composable 避免重复代码

## 精简 06-路由规范 Rule + 创建路由代码文件

按「Rule 管约束，Skill 管怎么做」原则，将 `06-路由规范.md` 中的所有代码块移除，只保留约束和决策标准。Rule 从 210 行精简到约 55 行。

同时创建了实际代码文件：
- `src/constants/routes.ts` — 路径常量（当前只有 PAGE_HOME）
- `src/utils/route.ts` — router 工具函数（to / toTab / back / replace / relaunch）
- `src/utils/route-guard.ts` — 路由守卫空函数（setupRouteGuard，TODO 待实现）

## 新增 08-样式规范 Rule + 09-工具函数规范 Rule

**新增文件**：
- `.codebuddy/rules/08-样式规范.md` — SCSS 样式编写约束
- `.codebuddy/rules/09-工具函数规范.md` — utils 工具函数编写约束

**08-样式规范** 涵盖：
- `uni.scss` 全局变量清单（颜色、字号、间距、圆角）及使用原则
- 尺寸单位（rpx 为主，边框用 px）
- 布局方式（flex 优先）
- 样式组织（页面用 `page-<name>` 根容器，组件用 BEM 命名）
- 安全区域适配（底部 safe-area、顶部 status-bar-height）
- 禁止事项（硬编码已有变量值、内联样式、!important、嵌套超3层等）

**09-工具函数规范** 涵盖：
- 文件分类：基础设施类（route.ts、storage.ts）vs 纯工具类（format.ts、validate.ts）
- 已有工具函数清单（route.ts 的 router 对象、route-guard.ts 的 setupRouteGuard）
- 命名规范（format/is/validate/to/get/set 前缀）
- 导出方式（具名导出 + index.ts 统一再导出）
- 使用优先级：检查已有 → 检查 uni-app 内置 → 新增到 utils
- 什么放 utils / 什么不放的判断标准

**联动更新**：
- `create-page` Skill — 必读 Rule 新增 `08-样式规范.md` 和 `09-工具函数规范.md`，编写规范和检查清单补充样式变量、工具函数、安全区域相关项
- `create-component` Skill — 必读 Rule 新增 `08-样式规范.md`，检查清单补充 BEM 命名、变量使用、嵌套层级检查项

## 统一 Rule 文件格式为 .mdc（官方标准）

将 `.codebuddy/rules/` 下所有 10 个 Rule 文件从 `.md` 统一转为 `.mdc` 格式，符合 CodeBuddy 官方标准。

**转换清单**（全部完成）：
- `01-项目概述.md` → `01-项目概述.mdc`
- `02-编码规范.md` → `02-编码规范.mdc`
- `03-项目结构.md` → `03-项目结构.mdc`
- `04-组件规范.md` → `04-组件规范.mdc`
- `05-API规范.md` → `05-API规范.mdc`
- `06-路由规范.md` → `06-路由规范.mdc`
- `07-状态管理.md` → `07-状态管理.mdc`
- `08-样式规范.md` → `08-样式规范.mdc`
- `09-工具函数规范.md` → `09-工具函数规范.mdc`
- `13-多端适配.md` → `13-多端适配.mdc`

**Skill 引用路径同步更新**（4 个 Skill）：
- `create-api/SKILL.md` — 引用改为 `.mdc`
- `create-component/SKILL.md` — 引用改为 `.mdc`
- `create-page/SKILL.md` — 引用改为 `.mdc`
- `create-store/SKILL.md` — 引用改为 `.mdc`

**`03-项目结构.mdc`** 内部对 `06-路由规范` 的交叉引用也已更新为 `.mdc`。
