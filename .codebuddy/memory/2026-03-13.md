# 2026-03-13 工作日志

## 接入 API：云备份私钥
- **服务**: did-app
- **接口路径**: POST `/auth/privateKeyBackup`
- **修改文件**:
  - `src/http/services/did-app/api-types.ts` — 新增 `DidappPrivateKeyBackupBody` 类型
  - `src/http/services/did-app/index.ts` — 新增 `didappPrivateKeyBackup` 函数，归入"私钥管理模块"
- **公共参数排除**: `x-token`（由 `requestAuthHeaderInterceptor` 处理）、`equipmentType`（由 `requestCommonHeaderInterceptor` 处理）
- **响应无 data**，泛型使用 `null`

## 优化 SKILL.md：去重 + 单一数据源 + 兜底机制
- **优化策略**: 方案 A（硬写）+ 兜底提醒。Rule 作为单一数据源，Skill 引用 Rule
- **删除的重复内容**:
  - 已有服务一览表格 → 改为引用 Rule「一、架构总览 → 已有服务」
  - 网关配置数据源表格 → 改为引用 Rule「二、网关配置」
  - 拦截器选配规则表格 → 改为引用 Rule「3.1 client.ts」
  - 类型/函数命名的服务前缀对照表 → 保留命名规则，前缀表引用 Rule
- **新增兜底机制**: 若 `src/http/services/` 下存在 Rule 未列出的服务目录，AI 会先读取 `client.ts` 获取信息
- **文件行数**: 568 → 511 行（减少约 10%）

## 接入 API：主管理员审核（发送 EnKey）
- **服务**: did-app
- **接口路径**: POST `/auth/sendEnKey`
- **接口描述**: 从消息里面获取信息（名称、手机号邮箱、公钥），主管理员审核子管理员的请求
- **修改文件**:
  - `src/http/services/did-app/api-types.ts` — 新增 `DidappSendEnKeyBody` 类型（status, enKey, pubKey, phoneOrEmail）
  - `src/http/services/did-app/index.ts` — 新增 `didappSendEnKey` 函数，归入"管理员审核模块"
- **公共参数排除**: `x-token`（header，由拦截器处理）、`equipmentType`（header，由拦截器处理）
- **响应无 data**，泛型使用 `null`

## 完善项目 Rules：01/02/03
- **参考模板**: `test/01-项目概述.md`、`test/02-编码规范.md`、`test/03-项目结构.md`
- **实际创建的文件**:
  - `.codebuddy/rules/01-项目概述.md` — 基于项目实际技术栈重写（Vue3.4+TS4.9+uni-app3.0+Pinia3+TDesign+Vite5）
  - `.codebuddy/rules/02-编码规范.md` — 新建，覆盖 TS 规范、Vue3 Composition API、Pinia Store、命名规范、格式化、条件编译、禁止事项
  - `.codebuddy/rules/03-项目结构.md` — 新建，覆盖完整目录结构、各目录规范、文件命名规则、pages.json 路由配置、main.ts 入口规范
- **关键差异（vs test 模板）**: 所有内容以项目实际状态为准而非照搬模板。例如：API 目录改为 `http/`（非 `api/`），状态管理目录是 `store/`（非 `stores/`），`verbatimModuleSyntax` 要求 import type 分离等
