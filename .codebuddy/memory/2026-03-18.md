# 2026-03-18

## 样式体系改造：以 TDesign CSS Variables 为主

- `src/main.ts` 新增引入 `@tdesign/uniapp/dist/common/style/theme/index.css`，使 `--td-xxx` CSS Variables 全局可用
- `08-样式规范.mdc` 全面重写变量体系部分：
  - 废弃 `$uni-xxx` SCSS 变量在业务代码中的使用，仅保留供 uni 生态第三方插件消费
  - 所有自定义组件/页面样式统一使用 `var(--td-xxx)` CSS Variables
  - 新增 TDesign 完整变量参考表：主题色、文字色、背景色、边框色、字号、间距、圆角、阴影
  - 新增自定义主题覆盖方式说明
- `02-编码规范.mdc` 之前已改为 `alwaysApply: true`（Always 类型）
