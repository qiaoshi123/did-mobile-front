/**
 * 公共组件统一导出
 * @desc 所有 `src/components/` 下的可复用组件在此聚合导出
 *       业务层通过 `@/components` 导入组件
 *       每条导出须附带 JSDoc 注释说明组件用途
 *
 *       当前暂无公共组件，新增组件时在此文件中添加导出
 *
 * @example
 * ```ts
 * // 新增组件导出示例：
 * // /** 用户头像卡片组件 *\/
 * // export { default as UserCard } from './user-card/index.vue'
 * ```
 */

export {}
